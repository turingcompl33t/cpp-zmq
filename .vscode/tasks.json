{
    "tasks": [
        {
            "type": "shell",
            "label": "debug build active file",
            "command": "${workspaceFolder}\\_build.bat",
            "args": [
                "${file}",
                "--debug"
            ]
        }
    ],
    "version": "2.0.0"
}